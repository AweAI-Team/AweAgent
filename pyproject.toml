[project]
name = "awe-agent"
version = "0.1.0"
description = "A unified, composable framework to build, evaluate, and train agents"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0,<3.0",
    "openai>=1.66.0,<3.0.0",
    "aiohttp>=3.9",
    "pyyaml>=6.0",
    "tenacity>=8.2,<10",
    "tiktoken>=0.7.0",
    "structlog>=24.0",
    "docker>=7.0",
    "anthropic>=0.30.0",           # Anthropic (Claude) LLM backend
    "sandbox_fusion",              # IterResearch PythonInterpreter tool (SandboxFusion client)
    "json5",                       # lenient parsing of string-encoded tool args
    "huggingface_hub",             # benchmark dataset downloads (BeyondSWE / ScaleSWE)
]

[project.optional-dependencies]
# Volcengine Ark LLM backend — heavy / region-specific SDK, kept optional.
ark = ["volcengine-python-sdk[ark]"]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.10",
]

[project.scripts]
awe-agent = "awe_agent.cli:main"

[project.entry-points."awe_agent.llm_backend"]
openai = "awe_agent.core.llm.backends.openai:OpenAIBackend"
azure = "awe_agent.core.llm.backends.openai:OpenAIBackend"
ark = "awe_agent.core.llm.backends.ark:ArkBackend"
sglang = "awe_agent.core.llm.backends.sglang:SGLangBackend"
anthropic = "awe_agent.core.llm.backends.anthropic:AnthropicBackend"
openai_response = "awe_agent.core.llm.backends.openai_response:OpenAIResponseBackend"

[project.entry-points."awe_agent.runtime"]
docker = "awe_agent.core.runtime.docker:DockerRuntime"

[project.entry-points."awe_agent.agent"]
search_swe = "awe_agent.scaffold.search_swe.agent:SearchSWEAgent"
deepsearch = "awe_agent.scaffold.deepsearch.agent:DeepSearchAgent"
terminus_2 = "awe_agent.scaffold.terminus_2.agent:Terminus2Agent"
iter_research = "awe_agent.scaffold.iter_research.agent:IterResearchAgent"

[project.entry-points."awe_agent.evaluator"]
beyond_swe = "awe_agent.tasks.beyond_swe.evaluator:BeyondSWEEvaluator"
terminal_bench_v2 = "awe_agent.tasks.terminal_bench_v2.evaluator:TerminalBenchV2Evaluator"
browsecomp = "awe_agent.tasks.browsecomp.evaluator:BrowseCompEvaluator"

[project.entry-points."awe_agent.search_backend"]
serpapi = "awe_agent.core.tool.search.backends.search.serpapi:SerpAPIBackend"

[project.entry-points."awe_agent.reader_backend"]
jina = "awe_agent.core.tool.search.backends.reader.jina:JinaReaderBackend"

[project.entry-points."awe_agent.tool"]
execute_bash = "awe_agent.core.tool.code.bash:ExecuteBashTool"
str_replace_editor = "awe_agent.core.tool.code.editor:StrReplaceEditorTool"
think = "awe_agent.core.tool.code.think:ThinkTool"
finish = "awe_agent.core.tool.code.finish:FinishTool"
web_search = "awe_agent.core.tool.search:WebSearchTool"
web_fetch = "awe_agent.core.tool.search:WebFetchTool"
web_fetch_raw = "awe_agent.core.tool.search:WebFetchRawTool"
python_interpreter = "awe_agent.core.tool.public.python_interpreter:PythonInterpreterTool"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.ruff.lint.per-file-ignores]
# Prompt modules hold verbatim, model-facing text that must not be reflowed.
"awe_agent/scaffold/*/prompts.py" = ["E501"]
"awe_agent/scaffold/*/prompts/*.py" = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.11"
strict = true
